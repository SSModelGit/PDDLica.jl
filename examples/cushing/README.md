# Cushing adaptation

Two persistent workers must make progress over the same physical interval. The
example represents required overlap using simultaneous start methods and active
continuous processes rather than inherited durative actions.
